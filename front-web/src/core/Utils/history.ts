import { createBrowserHistory } from 'history';

export default createBrowserHistory();

// Não importa se for um componente ou um arquivo typescript puro, quem chamar esse history podera navegar entre as rotas, fazer redirecionamentos.