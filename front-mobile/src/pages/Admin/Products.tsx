import React from "react";
import { View, Text } from "react-native";

const Products = () => {
    return (
        <View>
            <Text>Tela de Produtos (tela temporária)</Text>
        </View>
    )
}

export default Products;